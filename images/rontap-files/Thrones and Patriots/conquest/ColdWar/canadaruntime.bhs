conquest
{

static int diff = get_difficulty();
static int who;

run_once {
  ctw_add_objective_text($S("Capture the enemy capital."), "Hint_id", 0);
  for (i = 0; i < num_type_upgrade(2, "Airbase"); i++) {
    give_good(2, "Wealth", 400);
    give_good(2, "Oil", 400);
    build_id = find_build(2, "Airbase");
    train_unit_at_with_cost(2, 2, "Fighter", build_id);
  }
}

if (time() >= get_time_limit()) {
  defeat(1);
}

}